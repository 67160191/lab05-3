# 📘 JavaScript Fundamentals: Code Explanation & Results

เอกสารฉบับนี้จัดทำขึ้นเพื่ออธิบายหลักการทำงานของโค้ด JavaScript ในแต่ละหัวข้อ ตั้งแต่พื้นฐานการจัดการตัวแปรไปจนถึงการสร้างแอปพลิเคชันจำลอง

---

## 1. Variables & Data Types [`01-variables.js`](01-variables.js)

### 🔍 6. Challenge: Create a Person Object

เป็นการสร้าง Object ที่มีความซับซ้อนขึ้น โดยมีการใช้ **Methods** (ฟังก์ชันที่ทำงานอยู่ภายใน Object) เพื่อจัดการข้อมูลของตัวเอง

- **หลักการทำงาน:** โค้ดสร้าง `student` object ที่เก็บทั้งข้อมูลพื้นฐาน (Strings, Numbers) และข้อมูลแบบชุด (Array) รวมถึงมีการใช้คีย์เวิร์ด `this` เพื่อเข้าถึง Property ภายในตัวเองผ่านฟังก์ชัน `getFullName` และ `getInfo`
- **ผลลัพธ์ (Output):**
  ```text
  Student object: { firstName: 'Alice', lastName: 'Smith', ... }
  Full name: Alice Smith
  Info: Alice Smith, Age: 20, GPA: 3.8
  Courses: HTML, CSS, JavaScript
  ```

---

## 2. Functions & Arrow Functions [`02-functions.js`](02-functions.js)

### 🔍 8. Returning Objects

การเขียนฟังก์ชันให้ส่งค่ากลับมาเป็น Object (Factory Function) เพื่อใช้ในการสร้างข้อมูลที่มีโครงสร้างเดียวกันหลายๆ ชุด

- **หลักการทำงาน:** ใช้ความสามารถของ **Shorthand Property** เช่น `firstName` (แทน `firstName: firstName`) และการคำนวณค่า (Computed value) อย่างการนำชื่อมาสร้าง Email อัตโนมัติด้วย `.toLowerCase()`
- **ผลลัพธ์ (Output):**
  ```text
  {
    firstName: 'John',
    lastName: 'Doe',
    email: 'john.doe@example.com',
    ...
  }
  ```

### 🔍 9. Function as Parameter (Callback)

แสดงการทำงานแบบ **Higher-Order Function** ที่รับฟังก์ชันอื่นเข้ามาเป็นพารามิเตอร์

- **หลักการทำงาน:** ฟังก์ชัน `processArray` จะรับ Array และ Callback Function จากนั้นจะวนลูปนำทุกค่าใน Array ไปประมวลผลตาม Logic ที่ Callback กำหนด (เช่น การคูณ 2 หรือ ยกกำลัง)
- **ผลลัพธ์ (Output):**
  ```text
  Original: [1, 2, 3, 4, 5]
  Doubled: [2, 4, 6, 8, 10]
  Squared: [1, 4, 9, 16, 25]
  ```

---

## 3. Control Flow & Logic [`03-control-flow.js`](03-control-flow.js)

### 🔍 5. Short-Circuit Evaluation

เทคนิคการใช้ตัวดำเนินการทางตรรกะเพื่อช่วยให้โค้ดสั้นลงและปลอดภัยขึ้น (Defensive Programming)

- **หลักการทำงาน:** \* ใช้ `?.` (Optional Chaining) เพื่อไม่ให้โปรแกรม Error หากตัวแปรเป็น null
  - ใช้ `||` (OR) เพื่อกำหนดค่า Default หากตัวแปรด้านซ้ายเป็นค่าว่าง (Falsy)
- **ผลลัพธ์ (Output):**
  ```text
  User name: John
  User profile: undefined
  ```

### 🔍 7. Form Validation

การประยุกต์ใช้ Logic เพื่อตรวจสอบความถูกต้องของข้อมูล (Data Integrity)

- **หลักการทำงาน:** สร้างเงื่อนไขตรวจสอบ `formData` แต่ละฟิลด์ ถ้าพบข้อผิดพลาดจะทำการ `.push()` ข้อความเข้าไปใน Array `errors` หาก Array นี้ว่าง (length === 0) แสดงว่าข้อมูลผ่านการตรวจสอบ
- **ผลลัพธ์ (Output):**
  ```text
  Valid user: { isValid: true, errors: [] }
  Invalid user: { isValid: false, errors: ["Name must be...", "Must be 18 or older", ...] }
  ```

---

## 4. Loops & Array Methods [`04-loops.js`](04-loops.js)

### 🔍 9. Chaining methods

การนำ Array Methods มาต่อกัน (Chain) เพื่อประมวลผลข้อมูลเป็นทอดๆ (Functional Programming style)

- **หลักการทำงาน:** เริ่มจาก `filter` (กรองเฉพาะเลขคู่) -> `map` (นำมาคำนวณยกกำลังและเขียนเป็น String) -> `join` (รวมกลับเป็นข้อความเดียว)
- **ผลลัพธ์ (Output):**
  ```text
  Even numbers squared: 2²=4, 4²=16, 6²=36, 8²=64, 10²=100
  ```

### 🔍 10. Challenge: Student Grades

การใช้ Array Methods ขั้นสูงจัดการข้อมูลในรูปแบบตาราง (Object Array)

- **หลักการทำงาน:** ใช้ `map` เพื่อแจกเกรด, `reduce` เพื่อหาค่าเฉลี่ยและหาคนที่คะแนนสูงสุด (Top Scorer) และใช้ `sort` เพื่อเรียงลำดับข้อมูลตามคะแนน
- **ผลลัพธ์ (Output):**
  ```text
  Names: Alice, Bob, Charlie, Diana, Eve
  Class average: 87.00
  Top scorer: Alice (95)
  ```

---

## 5. Integration - Quiz Application [`05-integration.js`](05-integration.js)

### 🎯 Quiz Application logic

ส่วนนี้คือการรวมทุกหัวข้อ (Variables, Functions, Control Flow, Loops) เข้ามาทำงานร่วมกันในโปรแกรมเดียว

- **หลักการทำงาน:**
  1.  **Data:** เก็บโจทย์ใน Array of Objects
  2.  **Simulation:** ใช้ `forEach` วนลูปข้อสอบ และใช้ `Math.random` สุ่มเลือกคำตอบ
  3.  **Analysis:** ใช้ `filter` เพื่อนับคะแนนที่ถูก และคำนวณเกรดด้วย `if-else`
  4.  **Reporting:** สรุปผลลัพธ์ทั้งหมดออกมาทาง Console อย่างเป็นระเบียบ
- **ผลลัพธ์ (Output):**

  ```text
  QUIZ RESULTS:
  ------------------------------------------------------------
  Q1: What is 5 + 3?
    Your answer: 8
    ✅ CORRECT

  FINAL SCORE: 4/5 (80.0%)
  GRADE: B
  FEEDBACK: 👍👍 Great job!
  ```

---
